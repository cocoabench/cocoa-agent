# Solution

Using the directed edge list in `graph_task_spec_graph_shortest_path_html_2.html`, compute the shortest path from `v1` to `v8`.

The minimum route is:

- `v1 -> v2 -> v5 -> v7 -> v8`
- Total weight = `1 + 3 + 2 + 4 = 10`

Final:

```text
<answer>
PATH: v1->v2->v5->v7->v8
TOTAL_WEIGHT: 10
</answer>
```
