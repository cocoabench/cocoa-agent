# Solution

From the directed weighted edges in `graph_task_spec_graph_shortest_path_html_3.html`, compute the shortest path from `v1` to `v8`.

The minimum route is:

- `v1 -> v4 -> v5 -> v8`
- Total weight = `2 + 3 + 4 = 9`

Final:

```text
<answer>
PATH: v1->v4->v5->v8
TOTAL_WEIGHT: 9
</answer>
```
