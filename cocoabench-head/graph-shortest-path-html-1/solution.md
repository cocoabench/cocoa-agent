# Solution

Read the directed edges and weights from `graph_task_spec_graph_shortest_path_html_1.html`, then run shortest-path logic from `v1` to `v8` (e.g., Dijkstra).

The minimum route is:

- `v1 -> v3 -> v4 -> v6 -> v8`
- Total weight = `2 + 2 + 3 + 4 = 11`

Final:

```text
<answer>
PATH: v1->v3->v4->v6->v8
TOTAL_WEIGHT: 11
</answer>
```
